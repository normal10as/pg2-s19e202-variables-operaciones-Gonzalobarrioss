Imports System

Module ecuacion
    Sub Main(args As String())
        Dim a As Integer = 1
        Dim b As Integer = 5
        Dim c As Integer = 2
        Console.WriteLine("el resultado es: " & (b ^ 2 - 4 * a * c) / 2 * a)
    End Sub
End Module
